---
phase: 01-menu-dsl-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
  - src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
  - src/main/resources/menus/main.yml
  - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
autonomous: true
requirements: [MENU-01, MENU-02]
user_setup: []
must_haves:
  truths:
    - "Server owners can define a menu with title, layout, fill, and buttons in one YAML file."
    - "Server owners can use head, click, and inline input as the default short button syntax."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt"
      provides: "Canonical YAML parsing for layout, fill, head, click, and inline input."
      contains: "buttonSection.getStringList(\"click\")"
    - path: "src/main/resources/menus/main.yml"
      provides: "Bundled canonical example using fill, head, click, and input."
      contains: "input:"
    - path: "src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt"
      provides: "Regression coverage for MENU-01 and MENU-02."
      min_lines: 40
  key_links:
    - from: "src/main/resources/menus/main.yml"
      to: "src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt"
      via: "canonical YAML keys parsed by the repository"
      pattern: "buttonSection.getConfigurationSection\\(\"input\"\\)"
---

<objective>
Stabilize the Phase 1 canonical DSL contract and install the minimum automated regression coverage for it.

Purpose: Lock the "shorter than TRMenu" happy path before Phase 2 adds more runtime behaviors.
Output: A canonical main menu example, parser normalization for that example, and DSL tests for MENU-01 and MENU-02.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-menu-dsl-foundation/01-CONTEXT.md
@.planning/phases/01-menu-dsl-foundation/01-RESEARCH.md
@.planning/phases/01-menu-dsl-foundation/01-VALIDATION.md
@src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
@src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
@src/main/resources/menus/main.yml
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 - establish a valid Java 21 Maven build path</name>
  <files>pom.xml</files>
  <read_first>pom.xml, .planning/phases/01-menu-dsl-foundation/01-VALIDATION.md, .planning/phases/01-menu-dsl-foundation/01-RESEARCH.md, README.md</read_first>
  <action>Before any Maven test execution, establish or switch to a Java 21 build path for this workspace because the default environment is Java 25 and the project enforcer blocks that runtime; set or export a Java 21 `JAVA_HOME` for the execution session, ensure Maven resolves to that JDK, verify `mvn -version` reports `Java version: 21`, and then run a lightweight Maven command `mvn -q -DskipTests help:evaluate -Dexpression=project.version -DforceStdout` to confirm the build path is usable before installing test dependencies or running test goals.</action>
  <verify>mvn -version</verify>
  <acceptance_criteria>
    - README.md contains `Java 21`
    - pom.xml contains `<version>[21,22)</version>`
    - `mvn -version` output contains `Java version: 21`
    - `mvn -q -DskipTests help:evaluate -Dexpression=project.version -DforceStdout` exits successfully
  </acceptance_criteria>
  <done>A repeatable Java 21 Maven execution path exists for the workspace and lightweight Maven commands run before any test or package goal.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 - install the Java 21 test harness for DSL validation</name>
  <files>pom.xml, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt</files>
  <read_first>pom.xml, .planning/phases/01-menu-dsl-foundation/01-VALIDATION.md, .planning/phases/01-menu-dsl-foundation/01-RESEARCH.md, src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt, src/main/kotlin/cc/keer/amenu/config/MenuModels.kt</read_first>
  <action>Add the Phase 1 Wave 0 test stack in pom.xml: add test-scope dependencies `org.junit.jupiter:junit-jupiter:6.0.3` and `org.mockbukkit.mockbukkit:mockbukkit-v1.21:4.108.0`; add `maven-surefire-plugin` so `mvn test` runs JUnit Jupiter under the Java 21 build path established in Task 1; create `src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt` with two test functions named `loads_core_layout_fill_and_buttons` and `parses_shorthand_head_click_and_inline_input`; load `AMenuPlugin` through MockBukkit and assert the parsed `MenuDefinition` / `ButtonDefinition` fields for `title`, `layout`, `rows`, default fill `#`, `head`, `click`, and inline `input` prompt registration.</action>
  <verify>mvn -Dtest=MenuRepositoryDslTest test</verify>
  <acceptance_criteria>
    - pom.xml contains `<artifactId>junit-jupiter</artifactId>` and `<version>6.0.3</version>`
    - pom.xml contains `<artifactId>mockbukkit-v1.21</artifactId>` and `<version>4.108.0</version>`
    - pom.xml contains `<artifactId>maven-surefire-plugin</artifactId>`
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt contains `fun loads_core_layout_fill_and_buttons()`
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt contains `fun parses_shorthand_head_click_and_inline_input()`
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt contains `MockBukkit.load(AMenuPlugin::class.java)`
  </acceptance_criteria>
  <done>Wave 0 validation dependencies exist, the DSL test file exists, and the targeted Maven test command passes under Java 21.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize the canonical DSL path and shorten the bundled main example</name>
  <files>src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt, src/main/kotlin/cc/keer/amenu/config/MenuModels.kt, src/main/resources/menus/main.yml</files>
  <read_first>src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt, src/main/kotlin/cc/keer/amenu/config/MenuModels.kt, src/main/resources/menus/main.yml, .planning/phases/01-menu-dsl-foundation/01-CONTEXT.md, README.md</read_first>
  <action>Keep advanced aliases as compatibility only, but make the Phase 1 canonical path explicit in code and in `src/main/resources/menus/main.yml`: `layout` stays the source of truth for menu height, `fill` backfills `#`, `head` remains the short form for head textures, `click` is the primary action list, and inline `input` auto-registers the prompt without requiring top-level `prompts`; rewrite `main.yml` to the compact layout `######### / ##H#C#L## / #########`, keep only the `H`, `C`, and `L` buttons in that top-level example, keep `C` as the inline-input skin button, keep `H` opening `history`, keep `L` dispatching `skins`, and remove the `R` / `A` refresh-admin showcase from the default first-screen example so the bundled example demonstrates only `title`, `layout`, `fill`, `buttons`, `head`, `click`, and inline `input`.</action>
  <verify>mvn -Dtest=MenuRepositoryDslTest test</verify>
  <acceptance_criteria>
    - src/main/resources/menus/main.yml contains `layout:` and the exact line `  - "##H#C#L##"`
    - src/main/resources/menus/main.yml contains `fill:`
    - src/main/resources/menus/main.yml contains `head:` and `click:`
    - src/main/resources/menus/main.yml contains `input:`
    - src/main/resources/menus/main.yml does not contain `visible-permission:`
    - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt contains `buttonSection.getStringList("click")`
    - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt contains `buttonSection.getConfigurationSection("input")`
  </acceptance_criteria>
  <done>The bundled main example shows the minimal Phase 1 DSL path and the repository still parses it through the canonical shorthand fields.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A Java 21 Maven execution path is established and `mvn -version` reports Java 21
- [ ] `mvn -Dtest=MenuRepositoryDslTest test` passes under Java 21
- [ ] `src/main/resources/menus/main.yml` is shorter than the previous advanced showcase and still demonstrates `fill + head + click + input`
- [ ] MENU-01 and MENU-02 are both covered by automated assertions in `MenuRepositoryDslTest`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new advanced Phase 2/3/4 features are added to the parser contract
- The canonical main example demonstrates only the Phase 1 happy path
- MENU-01 and MENU-02 have executable regression tests
  </success_criteria>

<output>
After completion, create `.planning/phases/01-menu-dsl-foundation/01-01-SUMMARY.md`
</output>
