---
phase: 01-menu-dsl-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pom.xml
  - README.md
  - src/main/resources/plugin.yml
  - src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt
  - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt
autonomous: true
requirements: [MENU-03]
user_setup: []
must_haves:
  truths:
    - "Players can open the default menu with /amenu."
    - "Players can open a named bundled menu with /amenu open <menuId>."
    - "Plugin metadata and first-screen docs describe AMenu as a general menu plugin, not a skin-only plugin."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt"
      provides: "Primary /amenu command routing for default, open, and reload."
      contains: "plugin.menuService.openDefaultMenu(player)"
    - path: "src/main/resources/plugin.yml"
      provides: "Primary command and compatibility alias metadata."
      contains: "amenu:"
    - path: "src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt"
      provides: "Regression coverage for MENU-03."
      min_lines: 40
    - path: "README.md"
      provides: "General menu-plugin positioning and canonical command docs."
      contains: "/amenu open <menuId>"
  key_links:
    - from: "src/main/resources/plugin.yml"
      to: "src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt"
      via: "plugin command metadata points to the amenu command surface"
      pattern: "openDefaultMenu\\(player\\)"
    - from: "README.md"
      to: "src/main/resources/plugin.yml"
      via: "documented primary command matches plugin metadata"
      pattern: "/amenu open <menuId>"
---

<objective>
Align the public command surface, plugin metadata, and first-screen documentation to the Phase 1 product positioning.

Purpose: Prevent the project from presenting itself as a skin plugin after the DSL contract is stabilized.
Output: MENU-03 command coverage, cleaned command metadata, and README wording that matches the actual product boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-menu-dsl-foundation/01-CONTEXT.md
@.planning/phases/01-menu-dsl-foundation/01-RESEARCH.md
@.planning/phases/01-menu-dsl-foundation/01-VALIDATION.md
@.planning/phases/01-menu-dsl-foundation/01-01-SUMMARY.md
@src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt
@src/main/resources/plugin.yml
@README.md
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 - add MENU-03 command regression coverage</name>
  <files>src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt</files>
  <read_first>.planning/phases/01-menu-dsl-foundation/01-VALIDATION.md, src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/resources/plugin.yml, pom.xml</read_first>
  <action>Create `src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt` with a test function named `opens_default_and_named_menu`; run this task only after reusing the Java 21 Maven build path established in `01-01`, then use MockBukkit to load `AMenuPlugin`, create a player, dispatch `/amenu` and `/amenu open history`, and assert both command paths return success and leave the player with an opened inventory/menu state that corresponds to the default menu and the named `history` menu respectively; keep the test focused on MENU-03 only and reuse the Phase 1 test dependencies already installed in `pom.xml`.</action>
  <verify>mvn -Dtest=AMenuCommandTest test</verify>
  <acceptance_criteria>
    - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt contains `fun opens_default_and_named_menu()`
    - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt contains `MockBukkit.load(AMenuPlugin::class.java)`
    - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt contains `/amenu open history`
    - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt contains `assertTrue`
  </acceptance_criteria>
  <done>The targeted MENU-03 command regression test exists and passes under Java 21.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse the public command surface to /amenu plus one compatibility alias</name>
  <files>pom.xml, src/main/resources/plugin.yml, src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt</files>
  <read_first>pom.xml, src/main/resources/plugin.yml, src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt, .planning/phases/01-menu-dsl-foundation/01-CONTEXT.md, .planning/PROJECT.md</read_first>
  <action>Keep `/amenu` as the only primary command and keep `skinmenu` as the only compatibility alias: update `src/main/resources/plugin.yml` so `aliases: [skinmenu]` replaces `[menux, skinmenu, skingui]`, keep usage `/amenu [reload|open <menuId>]`, keep the plugin description generic to menu engines, and update `pom.xml` description text to `A modern, low-config Minecraft menu plugin for Spigot, Paper, and planned Folia compatibility`; in `AMenuCommand.kt` preserve exactly the current command branches `reload`, `open`, and default-open, keep `NavigationMode.ROOT` for named menu opens, and do not add any skin-specific command entrypoints.</action>
  <verify>mvn -Dtest=AMenuCommandTest test</verify>
  <acceptance_criteria>
    - src/main/resources/plugin.yml contains `aliases: [skinmenu]`
    - src/main/resources/plugin.yml does not contain `menux`
    - src/main/resources/plugin.yml does not contain `skingui`
    - pom.xml contains `<description>A modern, low-config Minecraft menu plugin for Spigot, Paper, and planned Folia compatibility</description>`
    - src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt contains `plugin.menuService.openMenu(player, args[1], navigation = NavigationMode.ROOT)`
    - src/main/kotlin/cc/keer/amenu/command/AMenuCommand.kt contains `plugin.menuService.openDefaultMenu(player)`
  </acceptance_criteria>
  <done>The outward command surface is centered on `/amenu`, metadata is generic, and MENU-03 still works through automated coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the README first screen around the generic menu-engine contract</name>
  <files>README.md</files>
  <read_first>README.md, src/main/resources/plugin.yml, src/main/resources/menus/main.yml, .planning/phases/01-menu-dsl-foundation/01-CONTEXT.md, .planning/phases/01-menu-dsl-foundation/01-RESEARCH.md</read_first>
  <action>Rewrite the README introduction so it states that AMenu is a general Minecraft menu plugin and that skin menus are bundled examples only; document the primary commands `/amenu`, `/amenu open <menuId>`, and `/amenu reload`; keep the minimal canonical YAML block synchronized with `src/main/resources/menus/main.yml`; add a short Phase 1 contract note that `templates`, `prompts`, `permission`, `visible-permission`, and `deny-actions` remain supported as advanced optional layers instead of the default path; keep the Java 21 build note and remove wording that describes the project as a skin-only plugin.</action>
  <verify>Get-Content README.md</verify>
  <acceptance_criteria>
    - README.md contains `/amenu`
    - README.md contains `/amenu open <menuId>`
    - README.md contains `skin menus are bundled examples only`
    - README.md contains `templates`
    - README.md contains `prompts`
    - README.md does not contain `skin menu plugin with efficient configuration`
  </acceptance_criteria>
  <done>The first-screen documentation matches the product boundary, the command surface, and the canonical Phase 1 DSL path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The Java 21 Maven execution path established in `01-01` is still active for this plan's test commands
- [ ] `mvn -Dtest=AMenuCommandTest test` passes under Java 21
- [ ] `mvn -Dtest=MenuRepositoryDslTest,AMenuCommandTest test` passes under Java 21
- [ ] Manual read of `README.md` and `src/main/resources/plugin.yml` confirms generic menu-plugin positioning and `/amenu` as the primary command
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No skin-specific primary command is reintroduced
- MENU-03 has automated command coverage
- README first-screen wording matches the general menu-plugin product boundary
  </success_criteria>

<output>
After completion, create `.planning/phases/01-menu-dsl-foundation/01-02-SUMMARY.md`
</output>
