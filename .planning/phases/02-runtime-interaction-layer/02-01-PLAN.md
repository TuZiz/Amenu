---
phase: 02-runtime-interaction-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
  - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
  - src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt
  - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt
  - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, INP-01, INP-02, INP-03, INP-04]
user_setup: []
must_haves:
  truths:
    - "Button click paths execute the declared runtime actions in order across close, open, back, refresh, player, console, message, and sound."
    - "Prompt sessions support reusable prompts and inline input, and handle submit, cancel, replace, and timeout safely."
    - "Async chat capture hands control back to the Bukkit/Paper safe thread boundary before running menu actions."
  artifacts:
    - path: "src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt"
      provides: "Regression coverage for runtime action execution, back stack behavior, and permission denial paths."
      min_lines: 80
    - path: "src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt"
      provides: "Regression coverage for prompt start, submit, cancel, replace, and timeout lifecycles."
      min_lines: 100
    - path: "src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt"
      provides: "Shared MockBukkit bootstrap, player helpers, slot lookup, and scheduler advancement for runtime tests."
      contains: "MockBukkit.load(AMenuPlugin::class.java)"
  key_links:
    - from: "src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      via: "runtime click assertions exercise MenuService.handleClick() and executeActions()"
      pattern: "fun handleClick\\("
    - from: "src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt"
      via: "prompt lifecycle assertions drive startPrompt(), async chat completion, and timeout cleanup"
      pattern: "fun startPrompt\\("
---

<objective>
Stabilize AMenu's runtime interaction layer with executable regression coverage for action chains and chat-input lifecycles.

Purpose: Turn the existing runtime implementation into a reliable, repeatable contract before Phase 2 expands showcase menus and docs.
Output: Java 21-safe runtime test harness, MenuService click-path regressions, and ChatInputService lifecycle regressions for RUN-* and INP-*.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-interaction-layer/02-CONTEXT.md
@.planning/phases/02-runtime-interaction-layer/02-RESEARCH.md
@.planning/phases/02-runtime-interaction-layer/02-VALIDATION.md
@src/main/kotlin/cc/keer/amenu/service/MenuService.kt
@src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
@src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt
@src/main/resources/config.yml
@src/main/resources/menus/history.yml
@src/main/resources/menus/admin.yml
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 - standardize the Java 21 runtime test path and shared MockBukkit harness</name>
  <files>pom.xml, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt</files>
  <read_first>pom.xml, .planning/phases/02-runtime-interaction-layer/02-VALIDATION.md, .planning/phases/02-runtime-interaction-layer/02-RESEARCH.md, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt, src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt</read_first>
  <action>Reuse the established Java 21 execution path for this workspace and make it explicit for Phase 2 runtime work: verify Maven is running under `.tools/jdk21` (or another Java 21 path) with `mvn -version`, then add a shared runtime test harness at `src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt` that boots MockBukkit, loads `AMenuPlugin`, creates test players, opens named menus, resolves layout characters to slots, and advances the scheduler for prompt timeout checks; if prompt-driving is ambiguous, do a minimal Wave 0 spike and lock the more stable path (`PlayerMock.chat()` or direct `AsyncChatEvent` construction) inside the harness before writing the main runtime regressions.</action>
  <verify>mvn -version</verify>
  <acceptance_criteria>
    - `mvn -version` reports `Java version: 21`
    - src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt contains `MockBukkit.load(AMenuPlugin::class.java)`
    - src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt contains a helper for scheduler tick advancement
    - src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt contains a helper for resolving a layout symbol to a slot index
  </acceptance_criteria>
  <done>The Phase 2 runtime tests share one stable Java 21-ready MockBukkit harness and no longer need ad hoc bootstrapping per test file.</done>
</task>

<task type="auto">
  <name>Task 2: Add MenuService runtime action regressions for navigation, side effects, and deny paths</name>
  <files>src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt</files>
  <read_first>src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, .planning/phases/02-runtime-interaction-layer/02-CONTEXT.md, .planning/phases/02-runtime-interaction-layer/02-RESEARCH.md, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt</read_first>
  <action>Create `src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt` and use the shared harness to cover the click-path contract in `MenuService`: verify `open`, `back`, `refresh`, and `close` preserve the intended menu state and back-stack semantics; verify `player`, `console`, `message`, and `sound` actions produce observable side effects through MockBukkit; verify action chains run in declaration order; verify `permission` and `deny-actions` prefer button-local denial feedback and only fall back to the configured `no-permission` system message when no custom deny chain exists; if the tests expose small runtime defects, patch `MenuService.kt` minimally without widening Phase 2 scope.</action>
  <verify>mvn "-Dtest=MenuRuntimeActionTest" test</verify>
  <acceptance_criteria>
    - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt contains `fun click_path_handles_open_back_refresh_and_close()`
    - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt contains `fun action_chain_executes_player_console_message_and_sound_in_order()`
    - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt contains `fun permission_denial_prefers_deny_actions_before_fallback_message()`
    - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt contains `handleClick(`
    - `mvn "-Dtest=MenuRuntimeActionTest" test` passes under Java 21
  </acceptance_criteria>
  <done>RUN-01, RUN-02, and RUN-03 are guarded by targeted runtime regressions that exercise real click paths rather than parser-only coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Add ChatInputService lifecycle regressions for prompt submit, cancel, replace, and timeout</name>
  <files>src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt, src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt</files>
  <read_first>src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt, src/main/resources/config.yml, src/main/resources/menus/main.yml, .planning/phases/02-runtime-interaction-layer/02-CONTEXT.md, .planning/phases/02-runtime-interaction-layer/02-RESEARCH.md, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt</read_first>
  <action>Create `src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt` and cover the full prompt lifecycle: start a reusable prompt and an inline button `input`, drive chat completion through the stable Wave 0 path, assert `{input}` is injected into submit actions, assert cancel keywords trigger cancel actions, assert starting a second prompt replaces the first session and emits replacement feedback, and assert timeout cleanup removes the session and notifies the player after scheduler advancement; keep the async-chat-to-main-thread handoff intact and patch `ChatInputService.kt` only if tests expose a concrete lifecycle bug.</action>
  <verify>mvn "-Dtest=ChatInputServiceTest" test</verify>
  <acceptance_criteria>
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt contains `fun reusable_prompt_and_inline_input_both_start_sessions()`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt contains `fun submit_injects_input_into_follow_up_actions()`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt contains `fun cancel_and_replace_paths_emit_expected_feedback()`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt contains `fun timeout_cleans_up_session_and_notifies_player()`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt contains either `player.chat(` or `AsyncChatEvent(`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt contains `performTicks(`
    - `mvn "-Dtest=ChatInputServiceTest" test` passes under Java 21
  </acceptance_criteria>
  <done>INP-01, INP-02, INP-03, and INP-04 are protected by lifecycle regressions that exercise prompt creation, replacement, submission, cancellation, and timeout behavior end to end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The Java 21 Maven execution path is active and `mvn -version` reports Java 21
- [ ] `mvn "-Dtest=MenuRuntimeActionTest" test` passes under Java 21
- [ ] `mvn "-Dtest=ChatInputServiceTest" test` passes under Java 21
- [ ] `mvn "-Dtest=MenuRuntimeActionTest,ChatInputServiceTest" test` passes under Java 21
- [ ] Async chat completion still returns to the Bukkit/Paper safe thread boundary before executing menu actions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new DSL or runtime action category is introduced
- RUN-01, RUN-02, RUN-03, INP-01, INP-02, INP-03, and INP-04 all have executable regression coverage
- The runtime implementation remains centered on `MenuService` and `ChatInputService`
  </success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-interaction-layer/02-01-SUMMARY.md`
</output>
