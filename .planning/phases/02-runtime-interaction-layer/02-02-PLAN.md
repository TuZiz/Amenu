---
phase: 02-runtime-interaction-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - README.md
  - src/main/resources/menus/history.yml
  - src/main/resources/menus/admin.yml
  - src/main/resources/menus/runtime.yml
  - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
  - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt
autonomous: true
requirements: [RUN-01, RUN-03, INP-01, INP-03, INP-04]
user_setup: []
must_haves:
  truths:
    - "The default main menu stays the shortest canonical DSL example even after Phase 2 runtime capabilities are exposed."
    - "Bundled example menus make permission denial, back navigation, refresh behavior, and prompt lifecycle discoverable without reading Kotlin source."
    - "Runtime showcase resources stay covered by automated parsing and targeted behavior checks."
  artifacts:
    - path: "src/main/resources/menus/history.yml"
      provides: "Secondary bundled example for back navigation and open flows."
      contains: "[back]"
    - path: "src/main/resources/menus/admin.yml"
      provides: "Secondary bundled example for permission, deny-actions, refresh, and explanatory feedback."
      contains: "permission:"
    - path: "README.md"
      provides: "Operator-facing runtime interaction documentation and example-menu guidance."
      contains: "Runtime interactions"
    - path: "src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt"
      provides: "Regression coverage that bundled runtime showcase menus continue to parse."
      min_lines: 60
  key_links:
    - from: "README.md"
      to: "src/main/resources/menus/history.yml"
      via: "docs point operators to bundled showcase menus instead of bloating main.yml"
      pattern: "history.yml|runtime.yml|admin.yml"
    - from: "src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt"
      to: "src/main/resources/menus/admin.yml"
      via: "parser coverage guards the runtime showcase resources"
      pattern: "admin"
---

<objective>
Expose Phase 2 runtime capabilities through bundled showcase menus and concise operator-facing docs without regressing the Phase 1 canonical DSL.

Purpose: Make the runtime interaction layer visible and teachable while keeping the default configuration path shorter than TRMenu.
Output: Bundled runtime showcase resources, parsing safeguards for those resources, and README guidance that points server owners to the right examples.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-interaction-layer/02-CONTEXT.md
@.planning/phases/02-runtime-interaction-layer/02-RESEARCH.md
@.planning/phases/02-runtime-interaction-layer/02-VALIDATION.md
@.planning/phases/02-runtime-interaction-layer/02-01-PLAN.md
@README.md
@src/main/resources/menus/main.yml
@src/main/resources/menus/history.yml
@src/main/resources/menus/admin.yml
@src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand bundled showcase menus without polluting the canonical main example</name>
  <files>src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, src/main/resources/menus/runtime.yml</files>
  <read_first>src/main/resources/menus/main.yml, src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, .planning/phases/02-runtime-interaction-layer/02-CONTEXT.md, .planning/phases/02-runtime-interaction-layer/02-RESEARCH.md</read_first>
  <action>Keep `src/main/resources/menus/main.yml` untouched as the shortest canonical example, and move all richer Phase 2 runtime teaching material into bundled secondary menus: extend `history.yml` and `admin.yml` or add a dedicated `runtime.yml` if clarity demands it; ensure the bundled examples visibly demonstrate `open`, `back`, `refresh`, `permission`, `deny-actions`, reusable `prompts`, inline `input`, cancel flow, and timeout guidance; keep the examples generic menu-engine showcases even if the sample actions still reference skin commands.</action>
  <verify>mvn "-Dtest=MenuRepositoryDslTest" test</verify>
  <acceptance_criteria>
    - src/main/resources/menus/main.yml still contains the exact line `  - "##H#C#L##"`
    - src/main/resources/menus/history.yml contains `[back]`
    - src/main/resources/menus/admin.yml contains `permission:`
    - src/main/resources/menus/admin.yml contains `deny-actions:`
    - If `src/main/resources/menus/runtime.yml` is created, it contains either `prompts:` or inline `input:`
    - No advanced runtime showcase keys are reintroduced into `src/main/resources/menus/main.yml`
  </acceptance_criteria>
  <done>The product now has bundled runtime showcase menus while the default main example stays short and beginner-friendly.</done>
</task>

<task type="auto">
  <name>Task 2: Add parser safeguards for bundled runtime showcase resources</name>
  <files>src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt</files>
  <read_first>src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt, src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, .planning/phases/02-runtime-interaction-layer/02-VALIDATION.md</read_first>
  <action>Extend `MenuRepositoryDslTest.kt` with a focused regression such as `loads_bundled_runtime_showcase_menus` that loads the bundled runtime example resources and asserts they parse into menus with the intended prompt/action/permission structures; if a dedicated `runtime.yml` is added, include it in the test; keep this coverage about resource integrity and contract shape rather than duplicating all runtime-behavior assertions already covered by `02-01`.</action>
  <verify>mvn "-Dtest=MenuRepositoryDslTest" test</verify>
  <acceptance_criteria>
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt contains `fun loads_bundled_runtime_showcase_menus()`
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt references `history`
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt references `admin`
    - If `runtime.yml` exists, the test file references `runtime`
    - `mvn "-Dtest=MenuRepositoryDslTest" test` passes under Java 21
  </acceptance_criteria>
  <done>Bundled runtime showcase menus are protected against YAML/resource regressions and remain aligned with the repository contract.</done>
</task>

<task type="auto">
  <name>Task 3: Document runtime interactions for server owners without drifting back to a skin-plugin identity</name>
  <files>README.md</files>
  <read_first>README.md, src/main/resources/menus/main.yml, src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, .planning/PROJECT.md, .planning/phases/02-runtime-interaction-layer/02-CONTEXT.md</read_first>
  <action>Add a concise `Runtime interactions` section to `README.md` that explains where server owners should look for bundled examples of back/refresh, permission denial feedback, and prompt lifecycle behaviors; explicitly state that `main.yml` remains the shortest canonical starting point while `history.yml`, `admin.yml`, and any dedicated `runtime.yml` carry the richer interaction demonstrations; preserve the general menu-engine positioning and do not rewrite the project as a skin-specific plugin.</action>
  <verify>mvn test</verify>
  <acceptance_criteria>
    - README.md contains `Runtime interactions`
    - README.md contains `main.yml`
    - README.md contains `history.yml`
    - README.md contains `admin.yml`
    - README.md still contains `skin menus are bundled examples only`
  </acceptance_criteria>
  <done>Server owners can discover the runtime interaction capabilities from the README and bundled examples without losing the short canonical entry path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/main/resources/menus/main.yml` remains the shortest canonical example and is not expanded with Phase 2 showcase content
- [ ] `mvn "-Dtest=MenuRepositoryDslTest" test` passes under Java 21
- [ ] `mvn test` passes under Java 21 after showcase resource and README updates
- [ ] Manual read confirms bundled examples clearly demonstrate runtime interactions and the README still positions AMenu as a general menu engine
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 2 runtime features are discoverable through bundled examples and docs
- The canonical `main.yml` example remains shorter than the richer showcase menus
- The project still presents itself as a general Minecraft menu plugin
  </success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-interaction-layer/02-02-SUMMARY.md`
</output>
