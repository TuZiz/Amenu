---
phase: 03-platform-compatibility-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt
  - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
  - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
  - src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt
  - src/main/kotlin/cc/keer/amenu/platform/TaskHandle.kt
  - src/main/kotlin/cc/keer/amenu/platform/BukkitPlatformScheduler.kt
  - src/main/kotlin/cc/keer/amenu/platform/FoliaPlatformScheduler.kt
  - src/main/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactory.kt
  - src/test/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactoryTest.kt
  - src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt
  - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceCompatibilityTest.kt
autonomous: true
requirements: [COMP-01, COMP-02]
user_setup: []
must_haves:
  truths:
    - "Players can still open the default menu, navigate to secondary menus, and trigger runtime actions safely after the compatibility handoff is introduced."
    - "Prompt submit, cancel, timeout, reload, and shutdown cleanup remain safe and observable after async chat input returns to the compatibility scheduler."
    - "One shaded plugin jar supports classic Spigot/Paper execution and Folia-aware scheduling without changing the menu DSL."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt"
      provides: "Single compatibility contract for player-bound and global runtime execution."
      contains: "interface PlatformScheduler"
    - path: "src/main/kotlin/cc/keer/amenu/platform/FoliaPlatformScheduler.kt"
      provides: "Reflective Folia execution path with Bukkit fallback."
      contains: "class FoliaPlatformScheduler"
    - path: "src/test/kotlin/cc/keer/amenu/service/ChatInputServiceCompatibilityTest.kt"
      provides: "Regression coverage for async chat handoff, timeout scheduling, and cleanup through the compatibility layer."
      min_lines: 80
  key_links:
    - from: "src/test/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactoryTest.kt"
      to: "src/main/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactory.kt"
      via: "factory assertions prove the runtime selects the correct scheduler path"
      pattern: "fun create\\("
    - from: "src/test/kotlin/cc/keer/amenu/service/ChatInputServiceCompatibilityTest.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt"
      via: "compatibility regressions exercise async handoff and timeout cleanup"
      pattern: "fun onAsyncChat\\("
---

<objective>
Introduce a narrow platform compatibility layer and route AMenu's runtime services through it.

Purpose: Remove hidden single-main-thread assumptions from the existing runtime while preserving the current DSL and action model.
Output: Platform scheduler package, runtime service refactor, and compatibility regressions for menu actions and prompt lifecycle behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-platform-compatibility-layer/03-CONTEXT.md
@.planning/phases/03-platform-compatibility-layer/03-RESEARCH.md
@.planning/phases/03-platform-compatibility-layer/03-VALIDATION.md
@src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt
@src/main/kotlin/cc/keer/amenu/service/MenuService.kt
@src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
@src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt
@src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the platform scheduler contract, factory, and Bukkit/Folia implementations</name>
  <files>src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt, src/main/kotlin/cc/keer/amenu/platform/TaskHandle.kt, src/main/kotlin/cc/keer/amenu/platform/BukkitPlatformScheduler.kt, src/main/kotlin/cc/keer/amenu/platform/FoliaPlatformScheduler.kt, src/main/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactory.kt, src/test/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactoryTest.kt</files>
  <read_first>src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt, .planning/phases/03-platform-compatibility-layer/03-CONTEXT.md, .planning/phases/03-platform-compatibility-layer/03-RESEARCH.md, .planning/phases/03-platform-compatibility-layer/03-VALIDATION.md, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt</read_first>
  <action>Create package `cc.keer.amenu.platform` with a concrete compatibility contract: `PlatformScheduler` defines `val isFolia`, `fun isPlayerThread(player: Player): Boolean`, `fun executeFor(player: Player, task: Runnable)`, `fun executeGlobal(task: Runnable)`, `fun runLaterFor(player: Player, delayTicks: Long, task: Runnable): TaskHandle`, and `fun runRepeatingFor(player: Player, delayTicks: Long, periodTicks: Long, task: Runnable): TaskHandle`; `TaskHandle` exposes `cancel()` plus `TaskHandle.NOOP`; `BukkitPlatformScheduler` uses classic Bukkit scheduler handoff; `FoliaPlatformScheduler` detects Folia reflectively through `Bukkit.getGlobalRegionScheduler()` / `Player#getScheduler()` / `Bukkit.isOwnedByCurrentRegion(...)` when present and falls back to the Bukkit implementation when reflective hooks are unavailable; `PlatformSchedulerFactory.create(plugin)` returns Folia or Bukkit implementation based on runtime capability detection; add `PlatformSchedulerFactoryTest.kt` with explicit tests for MockBukkit/classic runtime selection and task-handle behavior.</action>
  <verify>mvn "-Dtest=PlatformSchedulerFactoryTest" test</verify>
  <acceptance_criteria>
    - src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt contains `interface PlatformScheduler`
    - src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt contains `fun executeFor(player: Player, task: Runnable)`
    - src/main/kotlin/cc/keer/amenu/platform/TaskHandle.kt contains `val NOOP`
    - src/main/kotlin/cc/keer/amenu/platform/BukkitPlatformScheduler.kt contains `class BukkitPlatformScheduler`
    - src/main/kotlin/cc/keer/amenu/platform/FoliaPlatformScheduler.kt contains `class FoliaPlatformScheduler`
    - src/main/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactory.kt contains `fun create(plugin: JavaPlugin): PlatformScheduler`
    - src/test/kotlin/cc/keer/amenu/platform/PlatformSchedulerFactoryTest.kt contains `fun mockbukkit_runtime_uses_bukkit_scheduler()`
    - `mvn "-Dtest=PlatformSchedulerFactoryTest" test` passes under Java 21
  </acceptance_criteria>
  <done>AMenu now has one explicit platform-compatibility boundary instead of hidden scheduler assumptions spread across services.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor plugin bootstrap and runtime services to execute through the compatibility scheduler</name>
  <files>src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt</files>
  <read_first>src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt, src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt, .planning/phases/03-platform-compatibility-layer/03-CONTEXT.md, .planning/phases/03-platform-compatibility-layer/03-RESEARCH.md</read_first>
  <action>Update `AMenuPlugin.kt` to construct and retain `platformScheduler` from `PlatformSchedulerFactory` during `onEnable()`, keep it available to runtime services, and preserve the existing reload/shutdown lifecycle; update `MenuService.kt` so menu opening, back navigation, refresh reopening, and action-chain execution always enter through `platformScheduler.executeFor(player, ...)` when not already on the player-safe thread; update `ChatInputService.kt` so prompt timeout scheduling uses `platformScheduler.runLaterFor(player, settings.chatTimeoutSeconds * 20L, ...)`, async chat completion uses `platformScheduler.executeFor(player, ...)` instead of `plugin.server.scheduler.runTask(...)`, and reload/shutdown/session replacement cancel the returned `TaskHandle` rather than depending on raw `BukkitTask`; remove direct `plugin.server.scheduler.runTask` / `runTaskLater` usage from `ChatInputService.kt`.</action>
  <verify>mvn "-Dtest=MenuServiceCompatibilityTest,ChatInputServiceCompatibilityTest" test</verify>
  <acceptance_criteria>
    - src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt contains `lateinit var platformScheduler`
    - src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt contains `PlatformSchedulerFactory.create(this)`
    - src/main/kotlin/cc/keer/amenu/service/MenuService.kt contains `platformScheduler.executeFor`
    - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt contains `platformScheduler.runLaterFor`
    - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt contains `platformScheduler.executeFor`
    - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt does not contain `plugin.server.scheduler.runTask(`
    - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt does not contain `plugin.server.scheduler.runTaskLater(`
    - `mvn "-Dtest=MenuServiceCompatibilityTest,ChatInputServiceCompatibilityTest" test` passes under Java 21
  </acceptance_criteria>
  <done>The runtime is now routed through one compatibility-safe execution model for both classic and Folia-aware environments.</done>
</task>

<task type="auto">
  <name>Task 3: Add compatibility regressions for service handoff, prompt cleanup, and classic runtime safety</name>
  <files>src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt, src/test/kotlin/cc/keer/amenu/service/ChatInputServiceCompatibilityTest.kt, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt</files>
  <read_first>src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt, src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt, src/test/kotlin/cc/keer/amenu/service/ChatInputServiceTest.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt, .planning/phases/03-platform-compatibility-layer/03-VALIDATION.md</read_first>
  <action>Create `MenuServiceCompatibilityTest.kt` to assert the default open path and a click-driven open path still work after scheduler indirection, and create `ChatInputServiceCompatibilityTest.kt` to assert async chat completion, timeout expiry, prompt replacement, and shutdown/reload cleanup all operate with the compatibility scheduler in place; reuse `MenuPluginTestHarness.kt` rather than inventing a second bootstrap path; if the harness needs one new helper, add only the helper required to observe cleanup or scheduled callbacks under MockBukkit.</action>
  <verify>mvn "-Dtest=PlatformSchedulerFactoryTest,MenuServiceCompatibilityTest,ChatInputServiceCompatibilityTest" test</verify>
  <acceptance_criteria>
    - src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt contains `fun open_default_menu_still_succeeds_after_scheduler_handoff()`
    - src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt contains `fun click_actions_continue_to_open_secondary_menus()`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceCompatibilityTest.kt contains `fun async_chat_completion_runs_submit_actions_after_scheduler_handoff()`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceCompatibilityTest.kt contains `fun reload_and_shutdown_cancel_pending_prompt_tasks()`
    - src/test/kotlin/cc/keer/amenu/service/ChatInputServiceCompatibilityTest.kt contains either `player.chat(` or `AsyncChatEvent(`
    - `mvn "-Dtest=PlatformSchedulerFactoryTest,MenuServiceCompatibilityTest,ChatInputServiceCompatibilityTest" test` passes under Java 21
  </acceptance_criteria>
  <done>Phase 3 has focused regressions proving the scheduler abstraction preserves current runtime behavior while removing classic-thread assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn "-Dtest=PlatformSchedulerFactoryTest" test` passes under Java 21
- [ ] `mvn "-Dtest=MenuServiceCompatibilityTest,ChatInputServiceCompatibilityTest" test` passes under Java 21
- [ ] `mvn "-Dtest=PlatformSchedulerFactoryTest,MenuServiceCompatibilityTest,ChatInputServiceCompatibilityTest" test` passes under Java 21
- [ ] `ChatInputService.kt` no longer contains direct `plugin.server.scheduler.runTask` / `runTaskLater` calls
- [ ] The abstraction remains inside runtime/platform services and does not alter YAML DSL files
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `COMP-01` and `COMP-02` are implemented through a maintainable platform layer rather than one-off callsite patches
- Menu runtime behavior remains centered on `MenuService` and `ChatInputService`
- No new menu DSL keys or Phase 4 features are introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/03-platform-compatibility-layer/03-01-SUMMARY.md`
</output>
