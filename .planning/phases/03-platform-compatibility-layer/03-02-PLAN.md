---
phase: 03-platform-compatibility-layer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - README.md
  - src/main/resources/plugin.yml
  - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt
  - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt
  - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
  - .smoke/README.md
  - .smoke/paper-smoke.ps1
  - .smoke/folia-smoke.ps1
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
user_setup: []
must_haves:
  truths:
    - "Bundled example menus remain bundled examples of a general menu engine even after compatibility messaging is expanded."
    - "The repository states exactly what is proven by automated tests, packaging checks, and live Paper/Folia smoke."
    - "The shaded jar keeps direct Adventure runtime dependencies, including MiniMessage, inside the plugin artifact."
  artifacts:
    - path: ".smoke/README.md"
      provides: "Explicit Paper/Folia smoke workflow and manual verification boundary."
      contains: "MiniMessage"
    - path: "src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt"
      provides: "Regression coverage for bundled menus and command-entry compatibility."
      min_lines: 70
    - path: "README.md"
      provides: "Operator-facing compatibility matrix and general-product positioning."
      contains: "Compatibility"
  key_links:
    - from: ".smoke/README.md"
      to: "pom.xml"
      via: "smoke instructions depend on the Java 21 and shaded jar contract"
      pattern: "adventure-text-minimessage|maven-shade-plugin"
    - from: "src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt"
      to: "src/main/resources/menus/runtime.yml"
      via: "bundled-menu regression covers the secondary examples after compatibility changes"
      pattern: "runtime"
---

<objective>
Close the compatibility phase with bundled-example regressions, packaging proof, and operator-facing Paper/Folia smoke guidance.

Purpose: Make AMenu's compatibility claims explicit, testable, and aligned with the product's general menu-engine identity.
Output: Bundled compatibility regression coverage, smoke-run scripts/docs, packaging verification, and updated compatibility documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-platform-compatibility-layer/03-CONTEXT.md
@.planning/phases/03-platform-compatibility-layer/03-RESEARCH.md
@.planning/phases/03-platform-compatibility-layer/03-VALIDATION.md
@.planning/phases/03-platform-compatibility-layer/03-01-PLAN.md
@README.md
@pom.xml
@src/main/resources/plugin.yml
@src/main/resources/menus/main.yml
@src/main/resources/menus/history.yml
@src/main/resources/menus/admin.yml
@src/main/resources/menus/runtime.yml
@src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt
@src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bundled-menu and command-surface compatibility regressions</name>
  <files>src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt, src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt</files>
  <read_first>src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt, src/test/kotlin/cc/keer/amenu/support/MenuPluginTestHarness.kt, src/main/resources/menus/main.yml, src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, src/main/resources/menus/runtime.yml, .planning/phases/03-platform-compatibility-layer/03-RESEARCH.md</read_first>
  <action>Create `src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt` to verify the bundled menu set (`main`, `history`, `admin`, `runtime`) still loads and opens through the plugin after Phase 3 refactors; extend `AMenuCommandTest.kt` so `/amenu`, `/amenu open history`, and `/amenu reload` continue to work with the compatibility layer in place; extend `MenuRepositoryDslTest.kt` only where needed so bundled resource parsing coverage stays aligned with the compatibility examples rather than drifting back toward a skin-only menu story.</action>
  <verify>mvn "-Dtest=BundledMenuCompatibilityTest,AMenuCommandTest,MenuRepositoryDslTest" test</verify>
  <acceptance_criteria>
    - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt contains `fun bundled_examples_open_through_the_plugin_entrypoints()`
    - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt references `main`
    - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt references `history`
    - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt references `admin`
    - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt references `runtime`
    - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt contains `reload`
    - `mvn "-Dtest=BundledMenuCompatibilityTest,AMenuCommandTest,MenuRepositoryDslTest" test` passes under Java 21
  </acceptance_criteria>
  <done>The bundled examples and public command surface stay compatible after the scheduler refactor and remain protected by automated regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Add packaging proof and Paper/Folia smoke-run assets</name>
  <files>.smoke/README.md, .smoke/paper-smoke.ps1, .smoke/folia-smoke.ps1, pom.xml</files>
  <read_first>pom.xml, .planning/phases/03-platform-compatibility-layer/03-RESEARCH.md, .planning/phases/03-platform-compatibility-layer/03-VALIDATION.md, README.md</read_first>
  <action>Create `.smoke/README.md` plus `paper-smoke.ps1` and `folia-smoke.ps1` with a concrete Java 21 smoke path: build `target/amenu-1.0.0-SNAPSHOT-shaded.jar`, copy it into a Paper or Folia test server, launch the server headless, and inspect logs for successful plugin enablement and absence of `NoClassDefFoundError`, scheduler-reflection failures, or prompt cleanup exceptions; keep the packaging contract explicit by retaining the shaded Adventure MiniMessage dependency in `pom.xml` and verifying the packaged jar still contains `net/kyori/adventure/text/minimessage/MiniMessage.class` after `mvn package`.</action>
  <verify>mvn package; jar tf target/amenu-1.0.0-SNAPSHOT-shaded.jar | Select-String 'net/kyori/adventure/text/minimessage/MiniMessage.class'</verify>
  <acceptance_criteria>
    - .smoke/README.md contains `Paper`
    - .smoke/README.md contains `Folia`
    - .smoke/README.md contains `MiniMessage`
    - .smoke/paper-smoke.ps1 contains `amenu-1.0.0-SNAPSHOT-shaded.jar`
    - .smoke/folia-smoke.ps1 contains `amenu-1.0.0-SNAPSHOT-shaded.jar`
    - pom.xml contains `adventure-text-minimessage`
    - `mvn package` passes under Java 21
    - `jar tf target/amenu-1.0.0-SNAPSHOT-shaded.jar` contains `net/kyori/adventure/text/minimessage/MiniMessage.class`
  </acceptance_criteria>
  <done>The repository now proves both the packaging contract and the live smoke path required for real compatibility claims.</done>
</task>

<task type="auto">
  <name>Task 3: Update plugin metadata and README to publish the compatibility boundary without losing general-menu positioning</name>
  <files>README.md, src/main/resources/plugin.yml</files>
  <read_first>README.md, src/main/resources/plugin.yml, .planning/PROJECT.md, .planning/REQUIREMENTS.md, src/main/resources/menus/main.yml, src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, src/main/resources/menus/runtime.yml</read_first>
  <action>Add a `Compatibility` section to `README.md` that explicitly lists the supported-platform contract as `Spigot`, `Paper`, and `Folia-aware` execution with the automated-vs-manual proof boundary from Phase 3; preserve the line of thought that `main.yml` is the shortest canonical example while `history.yml`, `admin.yml`, and `runtime.yml` are bundled examples; update `src/main/resources/plugin.yml` to keep AMenu described as a general low-config menu plugin, preserve `/amenu` and `skinmenu` alias compatibility, and add the literal metadata line `folia-supported: true`.</action>
  <verify>mvn test</verify>
  <acceptance_criteria>
    - README.md contains `Compatibility`
    - README.md contains `Spigot`
    - README.md contains `Paper`
    - README.md contains `Folia`
    - README.md contains `skin menus are bundled examples only`
    - src/main/resources/plugin.yml contains `aliases: [skinmenu]`
    - src/main/resources/plugin.yml contains `folia-supported: true`
    - `mvn test` passes under Java 21
  </acceptance_criteria>
  <done>Operators can understand what AMenu supports, how it is verified, and why the bundled skin menus do not define the product.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn "-Dtest=BundledMenuCompatibilityTest,AMenuCommandTest,MenuRepositoryDslTest" test` passes under Java 21
- [ ] `mvn package` passes under Java 21
- [ ] `jar tf target/amenu-1.0.0-SNAPSHOT-shaded.jar` contains `net/kyori/adventure/text/minimessage/MiniMessage.class`
- [ ] `.smoke/README.md` clearly distinguishes automated proof from live Paper/Folia smoke steps
- [ ] README and plugin metadata still position AMenu as a general Minecraft menu plugin
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `COMP-01`, `COMP-02`, and `COMP-03` are all covered by tests, packaging proof, and operator-facing compatibility documentation
- Bundled examples remain examples and do not redefine AMenu as a skin-only plugin
- The recent MiniMessage packaging fix remains part of the compatibility contract
  </success_criteria>

<output>
After completion, create `.planning/phases/03-platform-compatibility-layer/03-02-SUMMARY.md`
</output>
