---
phase: 04-advanced-menu-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
  - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
  - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
  - src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt
  - src/main/resources/menus/main.yml
  - src/main/resources/menus/history.yml
  - src/main/resources/menus/runtime.yml
  - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
  - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt
  - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt
autonomous: true
requirements: [ADV-01]
user_setup: []
must_haves:
  truths:
    - "AMenu can render paged or list-oriented menus without forcing server owners to duplicate one YAML file per page."
    - "Async or delayed data-backed menu content returns through the existing platform-safe runtime path instead of mutating inventories off-thread."
    - "The bundled menus visibly demonstrate pagination and async rendering as part of the product showcase."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/config/MenuModels.kt"
      provides: "Paged/list menu configuration model additions."
      contains: "Page"
    - path: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      provides: "Runtime page state, list rendering, and async-safe refresh path."
      contains: "page"
    - path: "src/main/resources/menus/runtime.yml"
      provides: "Bundled lab that demonstrates modern input and runtime flows alongside advanced feature entrypoints."
      contains: "pagination"
  key_links:
    - from: "src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      via: "runtime tests prove page changes and list refresh remain safe"
      pattern: "page|openMenu"
---

<objective>
Implement the first visibly modern feature tier: pagination and async/list-backed menu rendering.

Purpose: move AMenu beyond static menus into data-shaped menus while preserving its low-config approach.
Output: menu pagination model, runtime page state, async-safe list refresh support, and bundled showcase coverage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-menu-features/04-CONTEXT.md
@.planning/phases/04-advanced-menu-features/04-RESEARCH.md
@.planning/phases/04-advanced-menu-features/04-VALIDATION.md
@src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
@src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
@src/main/kotlin/cc/keer/amenu/service/MenuService.kt
@src/main/resources/menus/main.yml
@src/main/resources/menus/history.yml
@src/main/resources/menus/runtime.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the DSL to describe paged/list-backed menu regions</name>
  <files>src/main/kotlin/cc/keer/amenu/config/MenuModels.kt, src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt</files>
  <action>Add compact config support for paged/list-driven regions, including the minimum metadata required to identify page slots, navigation controls, and item templates. Keep static menus untouched. Extend parser regressions to prove bundled and custom configs can declare these regions without bloating ordinary menus.</action>
  <verify>mvn "-Dtest=MenuRepositoryDslTest" test</verify>
  <acceptance_criteria>
    - `MenuModels.kt` defines a clear paged/list region model
    - `MenuRepository.kt` parses the new region config without breaking existing static menus
    - static `main.yml` style menus remain valid with no extra required keys
    - `mvn "-Dtest=MenuRepositoryDslTest" test` passes under Java 21
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement runtime page state and async-safe list refresh behavior</name>
  <files>src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt</files>
  <action>Add player/menu page state and list rendering behavior to the runtime service. Support page changes and async-backed refresh in a way that returns inventory mutation through the existing platform-safe execution path instead of updating GUI state off-thread.</action>
  <verify>mvn "-Dtest=MenuRuntimeActionTest" test</verify>
  <acceptance_criteria>
    - runtime page changes do not require per-page duplicated menu files
    - async data refresh returns to a safe render path
    - existing action chains and menu navigation continue to work
    - `mvn "-Dtest=MenuRuntimeActionTest" test` passes under Java 21
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Publish pagination and async-list showcase menus</name>
  <files>src/main/resources/menus/main.yml, src/main/resources/menus/history.yml, src/main/resources/menus/runtime.yml, src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt</files>
  <action>Upgrade the bundled showcase so the product visibly demonstrates page-oriented and async-backed menus. Keep the default menu readable, but expose a clear path into the advanced feature labs through bundled showcase entries and regression coverage.</action>
  <verify>mvn "-Dtest=BundledMenuCompatibilityTest" test</verify>
  <acceptance_criteria>
    - bundled menus visibly expose a pagination or list showcase path
    - `main.yml` remains a readable canonical hub rather than a business-specific example
    - regression coverage proves the new showcase menus still open through `/amenu`
    - `mvn "-Dtest=BundledMenuCompatibilityTest" test` passes under Java 21
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `mvn "-Dtest=MenuRepositoryDslTest" test` passes
- [ ] `mvn "-Dtest=MenuRuntimeActionTest" test` passes
- [ ] `mvn "-Dtest=BundledMenuCompatibilityTest" test` passes
- [ ] static menus still work without paged/list config
- [ ] async-backed rendering returns through the compatibility-safe runtime path
</verification>

<output>
After completion, create `.planning/phases/04-advanced-menu-features/04-01-SUMMARY.md`
</output>
