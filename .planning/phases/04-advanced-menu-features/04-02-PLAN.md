---
phase: 04-advanced-menu-features
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
  - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
  - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
  - src/main/resources/menus/main.yml
  - src/main/resources/menus/history.yml
  - src/main/resources/menus/admin.yml
  - src/main/resources/menus/runtime.yml
  - README.md
  - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
  - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt
  - src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt
autonomous: true
requirements: [ADV-02, ADV-03]
user_setup: []
must_haves:
  truths:
    - "Server owners can express visibility and state-driven rendering without duplicating whole menus."
    - "AMenu supports richer bindings or contextual openings beyond simple command entry, while keeping the general engine identity intact."
    - "The bundled showcase teaches conditions and advanced bindings as product features, not hidden config tricks."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt"
      provides: "Condition and binding DSL parsing."
      contains: "condition"
    - path: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      provides: "Runtime evaluation of visibility, state rendering, or contextual open paths."
      contains: "condition"
    - path: "README.md"
      provides: "Operator-facing explanation of advanced menu features."
      contains: "Pagination"
  key_links:
    - from: "src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      via: "runtime tests prove conditional visibility and binding behavior"
      pattern: "condition|permission|open"
---

<objective>
Implement conditions, state-driven rendering, and richer bindings as the second half of the modernization phase.

Purpose: let server owners build adaptive and contextual menus without abandoning AMenu's readable DSL.
Output: conditional render/binding support, showcase coverage, and updated docs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-menu-features/04-CONTEXT.md
@.planning/phases/04-advanced-menu-features/04-RESEARCH.md
@.planning/phases/04-advanced-menu-features/04-VALIDATION.md
@.planning/phases/04-advanced-menu-features/04-01-PLAN.md
@src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
@src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
@src/main/kotlin/cc/keer/amenu/service/MenuService.kt
@src/main/resources/menus/main.yml
@src/main/resources/menus/history.yml
@src/main/resources/menus/admin.yml
@src/main/resources/menus/runtime.yml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add declarative conditions and state-driven rendering support</name>
  <files>src/main/kotlin/cc/keer/amenu/config/MenuModels.kt, src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt, src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt</files>
  <action>Add a shared condition/render evaluation layer that can affect button visibility, display state, and action availability without forcing entire menu duplication. Keep this declarative and reusable across runtime rendering.</action>
  <verify>mvn "-Dtest=MenuRepositoryDslTest,MenuRuntimeActionTest" test</verify>
  <acceptance_criteria>
    - conditions can influence rendering or visibility without duplicating the entire menu
    - parser and runtime share the same condition model
    - existing permission semantics remain compatible
    - `mvn "-Dtest=MenuRepositoryDslTest,MenuRuntimeActionTest" test` passes under Java 21
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add richer bindings and contextual open paths</name>
  <files>src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt, src/test/kotlin/cc/keer/amenu/command/AMenuCommandTest.kt</files>
  <action>Add the first richer binding layer, such as event-driven or item/contextual opening paths, plus contextual variables passed into menu rendering. Keep the public `/amenu` path intact while allowing more modern triggers for advanced menus.</action>
  <verify>mvn "-Dtest=MenuRuntimeActionTest,AMenuCommandTest" test</verify>
  <acceptance_criteria>
    - advanced menus can be opened through at least one richer non-command binding path
    - contextual variables flow into rendering without breaking current `{input}` behavior
    - `/amenu` remains the canonical public entrypoint
    - `mvn "-Dtest=MenuRuntimeActionTest,AMenuCommandTest" test` passes under Java 21
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Publish condition and binding showcase plus operator docs</name>
  <files>src/main/resources/menus/main.yml, src/main/resources/menus/history.yml, src/main/resources/menus/admin.yml, src/main/resources/menus/runtime.yml, README.md</files>
  <action>Update bundled showcase menus and README so the product now clearly demonstrates advanced rendering, contextual openings, and bindings. Keep the product framed as a general modern menu engine.</action>
  <verify>mvn test</verify>
  <acceptance_criteria>
    - bundled showcase menus visibly teach conditions and richer bindings
    - README explains the advanced feature layer without reverting to a business-specific story
    - existing bundled example entrypoints remain intact
    - `mvn test` passes under Java 21
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `mvn "-Dtest=MenuRepositoryDslTest,MenuRuntimeActionTest" test` passes
- [ ] `mvn "-Dtest=MenuRuntimeActionTest,AMenuCommandTest" test` passes
- [ ] `mvn test` passes
- [ ] README and bundled menus teach advanced features as product capabilities
- [ ] `/amenu` remains the canonical entrypoint even after richer bindings are added
</verification>

<output>
After completion, create `.planning/phases/04-advanced-menu-features/04-02-SUMMARY.md`
</output>
