---
phase: 05-page-provider-placeholderapi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
  - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
  - src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt
  - src/main/kotlin/cc/keer/amenu/service/PlaceholderApiBridge.kt
  - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
  - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
  - src/main/kotlin/cc/keer/amenu/gui/MenuBindingListener.kt
  - src/main/resources/plugin.yml
  - src/test/kotlin/cc/keer/amenu/service/PlaceholderPipelineTest.kt
  - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
autonomous: true
requirements: [P5-02, P5-04]
user_setup: []
must_haves:
  truths:
    - "Static menus still load without any provider, cache, or update keys."
    - "Menus can declare provider/cache/update metadata through one compact DSL contract."
    - "Titles, lore, actions, binding placeholders, and provider params flow through one shared placeholder pipeline with safe PlaceholderAPI absence behavior."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/config/MenuModels.kt"
      provides: "Dynamic provider, cache, and update model contracts."
      contains: "Provider"
    - path: "src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt"
      provides: "Ordered placeholder resolution entrypoint used by runtime services."
      contains: "render"
    - path: "src/test/kotlin/cc/keer/amenu/service/PlaceholderPipelineTest.kt"
      provides: "Regression coverage for internal placeholders and optional PlaceholderAPI bridge behavior."
      contains: "PlaceholderPipelineTest"
  key_links:
    - from: "src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt"
      to: "src/main/kotlin/cc/keer/amenu/config/MenuModels.kt"
      via: "parser normalizes dynamic DSL into explicit provider/cache/update contracts"
      pattern: "provider|cache|update"
    - from: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt"
      via: "runtime uses the shared placeholder pipeline instead of local string replacement"
      pattern: "PlaceholderPipeline"
    - from: "pom.xml"
      to: "src/main/resources/plugin.yml"
      via: "PlaceholderAPI compile-time contract stays optional at runtime through `provided` + `softdepend`"
      pattern: "placeholderapi|softdepend"
---

<objective>
Lock the Phase 5 contracts before provider execution begins: the dynamic DSL, the shared placeholder pipeline, and the soft PlaceholderAPI bridge.

Purpose: make later provider/cache/runtime work build on one parser and one render contract instead of multiplying special cases.
Output: dynamic model/parser contracts, shared placeholder services, and RED/GREEN regression coverage for the new DSL and placeholder path.
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
@.planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
@.planning/phases/05-page-provider-placeholderapi/05-VALIDATION.md
@.planning/phases/04-advanced-menu-features/04-01-SUMMARY.md
@.planning/phases/04-advanced-menu-features/04-02-SUMMARY.md
@src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
@src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
@src/main/kotlin/cc/keer/amenu/service/MenuService.kt
@src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
@src/main/kotlin/cc/keer/amenu/gui/MenuBindingListener.kt
@pom.xml
@src/main/resources/plugin.yml
@src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
@src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt

<interfaces>
From `src/main/kotlin/cc/keer/amenu/config/MenuModels.kt`:
```kotlin
data class ButtonDefinition(
    val symbol: Char,
    val icon: IconDefinition,
    val actions: List<MenuAction>,
    val permission: String?,
    val visiblePermission: String?,
    val denyActions: List<MenuAction>,
    val conditions: List<MenuCondition>,
    val states: List<ButtonStateDefinition>,
)

data class PageRegionDefinition(
    val id: String,
    val symbol: Char,
    val entries: List<PageEntryDefinition>,
    val loadingIcon: IconDefinition,
    val emptyIcon: IconDefinition,
    val asyncDelayTicks: Long,
)
```

From `src/main/kotlin/cc/keer/amenu/service/MenuService.kt`:
```kotlin
fun openMenu(
    player: Player,
    menuId: String,
    placeholders: Map<String, String> = emptyMap(),
    navigation: NavigationMode = NavigationMode.NONE,
)

fun executeActions(
    player: Player,
    menuId: String,
    actions: List<MenuAction>,
    placeholders: Map<String, String> = defaultPlaceholders(player),
)
```

From `src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt`:
```kotlin
interface PlatformScheduler {
    fun executeFor(player: Player, task: Runnable)
    fun executeGlobal(task: Runnable)
    fun runLaterAsync(delayTicks: Long, task: Runnable): TaskHandle
    fun runRepeatingFor(player: Player, delayTicks: Long, periodTicks: Long, task: Runnable): TaskHandle
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for the dynamic DSL and shared placeholder pipeline</name>
  <files>src/test/kotlin/cc/keer/amenu/service/PlaceholderPipelineTest.kt, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt</files>
  <read_first>
    - .planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
    - .planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
    - .planning/phases/05-page-provider-placeholderapi/05-VALIDATION.md
    - src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
    - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
    - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
    - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
    - src/main/kotlin/cc/keer/amenu/gui/MenuBindingListener.kt
    - pom.xml
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
  </read_first>
  <behavior>
    - Test `parses_provider_cache_loading_empty_update_and_error_metadata_without_breaking_static_menus`: a YAML menu using `provider.type`, `provider.params`, `provider.cache.ttl`, `update.interval`, `loading`, `empty`, and `error` parses into explicit model contracts, including explicit `loadingIcon` and `emptyIcon` wiring, while bundled `menu.yml` still loads unchanged.
    - Test `shared_pipeline_resolves_internal_then_optional_external_placeholders`: `{player}` always resolves, `%player_name%` resolves only when the bridge reports PlaceholderAPI enabled, and raw `%player_name%` remains non-fatal when the bridge is unavailable.
  </behavior>
  <action>Create `PlaceholderPipelineTest.kt` and extend `MenuRepositoryDslTest.kt` with RED coverage before implementation. Use the exact DSL keys `provider.type`, `provider.params`, `provider.cache.ttl`, `update.interval`, `loading`, `empty`, and `error`. Keep the tests intentionally RED by asserting the new model and pipeline behavior before adding any production implementation. Per D-03, keep one assertion that bundled static menus still load with no provider metadata at all.</action>
  <verify>
    <automated>$env:JAVA_HOME='D:\codex\Amenu\.tools\jdk21'; $env:PATH=&quot;$env:JAVA_HOME\bin;$env:PATH&quot;; mvn -q -Dtest=PlaceholderPipelineTest,MenuRepositoryDslTest test; if ($LASTEXITCODE -eq 0) { throw 'Expected RED tests for Phase 5 Task 1 to fail before implementation.' }</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/kotlin/cc/keer/amenu/service/PlaceholderPipelineTest.kt` exists
    - `MenuRepositoryDslTest.kt` contains `provider.type`, `provider.cache.ttl`, and `update.interval`
    - `MenuRepositoryDslTest.kt` contains a static-menu regression that still loads bundled `menu`
    - the RED verify command fails because the production implementation does not exist yet
  </acceptance_criteria>
  <done>Wave 0 test scaffolds for P5-02 and P5-04 exist and fail for the expected missing implementation reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared placeholder pipeline, soft PlaceholderAPI bridge, and dynamic DSL contracts</name>
  <files>pom.xml, src/main/kotlin/cc/keer/amenu/config/MenuModels.kt, src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt, src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt, src/main/kotlin/cc/keer/amenu/service/PlaceholderApiBridge.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt, src/main/kotlin/cc/keer/amenu/gui/MenuBindingListener.kt, src/main/resources/plugin.yml, src/test/kotlin/cc/keer/amenu/service/PlaceholderPipelineTest.kt, src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt</files>
  <read_first>
    - .planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
    - .planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
    - src/test/kotlin/cc/keer/amenu/service/PlaceholderPipelineTest.kt
    - src/test/kotlin/cc/keer/amenu/config/MenuRepositoryDslTest.kt
    - src/main/kotlin/cc/keer/amenu/config/MenuModels.kt
    - src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt
    - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
    - src/main/kotlin/cc/keer/amenu/service/ChatInputService.kt
    - src/main/kotlin/cc/keer/amenu/gui/MenuBindingListener.kt
    - pom.xml
    - src/main/resources/plugin.yml
  </read_first>
  <action>Implement the exact Phase 5 contract chosen here: add explicit dynamic model objects to `MenuModels.kt` for provider metadata, cache TTL, update interval, and region `loading`, `empty`, and `error` presentation; teach `MenuRepository.kt` to parse `provider.type`, `provider.params`, `provider.cache.ttl`, `update.interval`, `loading`, `empty`, and `error`, while normalizing legacy `Pages.*.entries + async-delay` into the new provider model per D-03 and wiring `loadingIcon` / `emptyIcon` through the new contracts instead of leaving them as ad hoc parser leftovers; add PlaceholderAPI's repository and a `provided` dependency to `pom.xml`, then create `PlaceholderPipeline.kt` with one ordered `render(...)` entrypoint that resolves AMenu `{...}` placeholders first, then delegates `%...%` resolution through `PlaceholderApiBridge.kt` only when `plugin.server.pluginManager.isPluginEnabled(\"PlaceholderAPI\")` is true per D-05; replace the local string-replacement helpers in `MenuService.kt`, `ChatInputService.kt`, and `MenuBindingListener.kt` with this shared pipeline per D-06; add `softdepend: [PlaceholderAPI]` to `src/main/resources/plugin.yml`.</action>
  <verify>
    <automated>$env:JAVA_HOME='D:\codex\Amenu\.tools\jdk21'; $env:PATH=&quot;$env:JAVA_HOME\bin;$env:PATH&quot;; mvn -q -Dtest=PlaceholderPipelineTest,MenuRepositoryDslTest,MenuRuntimeActionTest test</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` declares PlaceholderAPI as a `provided` dependency with the required repository entry
    - `src/main/kotlin/cc/keer/amenu/config/MenuModels.kt` contains explicit provider/cache/update contracts
    - `src/main/kotlin/cc/keer/amenu/config/MenuModels.kt` contains explicit `loading`, `empty`, and `error` contracts for provider-backed regions or surfaces
    - `src/main/kotlin/cc/keer/amenu/config/MenuRepository.kt` parses `provider`, `cache`, `update`, `loading`, `empty`, and `error`
    - `src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt` exists and is referenced from `MenuService.kt`, `ChatInputService.kt`, and `MenuBindingListener.kt`
    - `src/main/resources/plugin.yml` contains `softdepend: [PlaceholderAPI]`
    - the automated verify command passes under the repo-local Java 21 toolchain
  </acceptance_criteria>
  <done>P5-02 and P5-04 are implemented through one shared parser/render contract, and PlaceholderAPI remains an optional non-fatal bridge.</done>
</task>

</tasks>

<verification>
- [ ] `PlaceholderPipelineTest` and `MenuRepositoryDslTest` exist before implementation and fail in RED mode
- [ ] `mvn -q -Dtest=PlaceholderPipelineTest,MenuRepositoryDslTest,MenuRuntimeActionTest test` passes under Java 21
- [ ] bundled static menus still load without provider metadata
- [ ] PlaceholderAPI remains optional and non-fatal when absent
</verification>

<success_criteria>
- The parser accepts dynamic provider metadata without forcing changes to ordinary static menus.
- Runtime string resolution converges on one shared placeholder pipeline.
- Phase 5 downstream plans can depend on explicit model contracts instead of ad hoc parser maps.
</success_criteria>

<output>
After completion, create `.planning/phases/05-page-provider-placeholderapi/05-01-SUMMARY.md`
</output>
