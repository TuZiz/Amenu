---
phase: 05-page-provider-placeholderapi
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - pom.xml
  - src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt
  - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
  - src/main/kotlin/cc/keer/amenu/service/provider/MenuDataProvider.kt
  - src/main/kotlin/cc/keer/amenu/service/provider/MenuProviderRegistry.kt
  - src/main/kotlin/cc/keer/amenu/service/provider/ProviderCache.kt
  - src/main/kotlin/cc/keer/amenu/service/provider/ProviderRequest.kt
  - src/main/kotlin/cc/keer/amenu/service/provider/ProviderResult.kt
  - src/main/kotlin/cc/keer/amenu/service/provider/builtin/EntriesPageProvider.kt
  - src/main/kotlin/cc/keer/amenu/service/provider/builtin/PlaceholderStateProvider.kt
  - src/test/kotlin/cc/keer/amenu/service/MenuDynamicProviderServiceTest.kt
  - src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt
autonomous: true
requirements: [P5-01, P5-05]
user_setup: []
must_haves:
  truths:
    - "Provider-backed page regions resolve through registered providers instead of hardcoded `MenuService` branches."
    - "Dynamic provider results are cached per viewer/menu/surface with TTL and explicit invalidation."
    - "Async provider completion rerenders through the platform-safe handoff path before any visible inventory mutation."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/service/provider/MenuProviderRegistry.kt"
      provides: "Registry for built-in provider implementations."
      contains: "register"
    - path: "src/main/kotlin/cc/keer/amenu/service/provider/ProviderCache.kt"
      provides: "Viewer/menu/surface-scoped cache facade with TTL and invalidation."
      contains: "invalidate"
    - path: "src/test/kotlin/cc/keer/amenu/service/MenuDynamicProviderServiceTest.kt"
      provides: "Regression coverage for cache hits, invalidation, and result-state transitions."
      contains: "MenuDynamicProviderServiceTest"
  key_links:
    - from: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/provider/MenuProviderRegistry.kt"
      via: "page-region rendering resolves providers through the registry"
      pattern: "providerRegistry"
    - from: "src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/provider/ProviderRequest.kt"
      via: "provider params are resolved through the shared pipeline before provider invocation"
      pattern: "provider.params|PlaceholderPipeline|ProviderRequest"
    - from: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      to: "src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt"
      via: "async provider completion returns through `executeFor` before rerender"
      pattern: "executeFor|renderOpenMenuIfCurrent"
---

<objective>
Implement the Phase 5 provider runtime: registry, cache facade, built-in providers, and safe page-region rerender.

Purpose: move page regions from static `entries` storage to a pluggable provider execution model without breaking Bukkit/Paper/Folia safety.
Output: provider registry, Caffeine-backed cache facade, provider-backed page-region runtime, and service-level regression coverage.
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
@.planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
@.planning/phases/05-page-provider-placeholderapi/05-VALIDATION.md
@.planning/phases/05-page-provider-placeholderapi/05-01-PLAN.md
@src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt
@src/main/kotlin/cc/keer/amenu/service/MenuService.kt
@src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt
@pom.xml
@src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt

<interfaces>
From `src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt`:
```kotlin
open class AMenuPlugin : JavaPlugin() {
    lateinit var menuRepository: MenuRepository
    lateinit var menuService: MenuService
    lateinit var platformScheduler: PlatformScheduler

    fun reloadPlugin()
}
```

From `src/main/kotlin/cc/keer/amenu/service/MenuService.kt`:
```kotlin
private fun renderOpenMenuIfCurrent(player: Player, menuId: String)
private fun stateFor(player: Player, menuId: String): MenuViewState
```

From `src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt`:
```kotlin
fun executeFor(player: Player, task: Runnable)
fun executeGlobal(task: Runnable)
fun runLaterAsync(delayTicks: Long, task: Runnable): TaskHandle
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for provider cache scope, invalidation, and scheduler-safe rerender</name>
  <files>src/test/kotlin/cc/keer/amenu/service/MenuDynamicProviderServiceTest.kt, src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt</files>
  <read_first>
    - .planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
    - .planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
    - .planning/phases/05-page-provider-placeholderapi/05-VALIDATION.md
    - .planning/phases/05-page-provider-placeholderapi/05-01-PLAN.md
    - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
    - src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt
    - src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt
    - src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt
  </read_first>
  <behavior>
    - Test `provider_results_are_cached_per_viewer_menu_surface_and_invalidated_by_refresh`: two opens from the same player/menu/surface hit cache, a targeted refresh invalidates only that surface, and another player's open uses a distinct cache key.
    - Test `async_provider_completion_rerenders_through_platform_safe_handoff`: provider work can start off-thread, but the visible rerender path still flows through the platform-safe scheduler before the inventory changes.
    - Test `provider_request_params_are_resolved_before_invocation`: a provider configured with params containing `{player}` and `%player_name%` receives those params already resolved through `PlaceholderPipeline`, and the absence path stays non-fatal when PlaceholderAPI is unavailable.
  </behavior>
  <action>Create `MenuDynamicProviderServiceTest.kt` and extend `MenuServiceCompatibilityTest.kt` with RED coverage using fake providers that count loads. Assert the internal cache key shape is viewer/menu/surface/provider-specific per D-07, and keep the tests RED until the registry/cache/runtime classes exist. Add one RED test fixture where `provider.params.viewer-name: "{player}"` and `provider.params.external-name: "%player_name%"`, then assert the fake provider receives a resolved `ProviderRequest` instead of raw tokens. Make one RED assertion that `Refresh` or `page: refresh <region>` invalidates only the targeted provider-backed region rather than clearing every menu cache entry.</action>
  <verify>
    <automated>$env:JAVA_HOME='D:\codex\Amenu\.tools\jdk21'; $env:PATH=&quot;$env:JAVA_HOME\bin;$env:PATH&quot;; mvn -q -Dtest=MenuDynamicProviderServiceTest,MenuServiceCompatibilityTest test; if ($LASTEXITCODE -eq 0) { throw 'Expected RED tests for Phase 5 Task 1 to fail before provider runtime implementation.' }</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/kotlin/cc/keer/amenu/service/MenuDynamicProviderServiceTest.kt` exists
    - the RED tests assert viewer/menu/surface cache scoping and targeted invalidation
    - `MenuServiceCompatibilityTest.kt` contains an async provider rerender assertion
    - the RED verify command fails because the provider runtime is not implemented yet
  </acceptance_criteria>
  <done>Wave 0 test scaffolds for P5-01 and P5-05 exist and fail for the expected missing runtime reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the provider registry, Caffeine cache facade, and safe provider-backed page runtime</name>
  <files>pom.xml, src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/kotlin/cc/keer/amenu/service/provider/MenuDataProvider.kt, src/main/kotlin/cc/keer/amenu/service/provider/MenuProviderRegistry.kt, src/main/kotlin/cc/keer/amenu/service/provider/ProviderCache.kt, src/main/kotlin/cc/keer/amenu/service/provider/ProviderRequest.kt, src/main/kotlin/cc/keer/amenu/service/provider/ProviderResult.kt, src/main/kotlin/cc/keer/amenu/service/provider/builtin/EntriesPageProvider.kt, src/main/kotlin/cc/keer/amenu/service/provider/builtin/PlaceholderStateProvider.kt, src/test/kotlin/cc/keer/amenu/service/MenuDynamicProviderServiceTest.kt, src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt</files>
  <read_first>
    - .planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
    - .planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
    - .planning/phases/05-page-provider-placeholderapi/05-01-PLAN.md
    - src/test/kotlin/cc/keer/amenu/service/MenuDynamicProviderServiceTest.kt
    - src/test/kotlin/cc/keer/amenu/service/MenuServiceCompatibilityTest.kt
    - src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt
    - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
    - src/main/kotlin/cc/keer/amenu/service/PlaceholderPipeline.kt
    - pom.xml
  </read_first>
  <action>Add `com.github.ben-manes.caffeine:caffeine:3.2.3` to `pom.xml`. Create the provider runtime under `src/main/kotlin/cc/keer/amenu/service/provider/` with the exact contract split below: `MenuDataProvider.kt` for the provider interface, `ProviderRequest.kt` for viewer/menu/surface/provider params, `ProviderResult.kt` with explicit `Success`, `Empty`, and `Error` states per D-16, `MenuProviderRegistry.kt` for built-in registrations, and `ProviderCache.kt` for TTL + invalidation. Register at least two built-ins per D-02 and the research recommendation: `EntriesPageProvider` to normalize the existing `entries + async-delay` behavior and `PlaceholderStateProvider` to supply provider-backed placeholder maps for later button-state reevaluation. Wire the provider param path explicitly: when `MenuService.kt` prepares provider invocation, it must resolve every `provider.params` value through `PlaceholderPipeline` first, then write the resolved map into `ProviderRequest.kt`, so providers never receive raw `{player}` or `%player_name%` tokens. Wire the registry/cache into `AMenuPlugin.kt` and `MenuService.kt`; provider execution may happen off-thread, but visible rerender must return through `platformScheduler.executeFor(player, ...)` and reuse `renderOpenMenuIfCurrent(...)` instead of unsafe inventory mutation per D-15. Implement targeted invalidation for `MenuAction.Refresh` and `PageOperation.REFRESH` so cache clearing is surface-specific rather than global per D-08.</action>
  <verify>
    <automated>$env:JAVA_HOME='D:\codex\Amenu\.tools\jdk21'; $env:PATH=&quot;$env:JAVA_HOME\bin;$env:PATH&quot;; mvn -q -Dtest=MenuDynamicProviderServiceTest,MenuServiceCompatibilityTest test</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` contains `com.github.ben-manes.caffeine:caffeine:3.2.3`
    - `MenuProviderRegistry.kt` registers `entries` and `placeholder-state`
    - `ProviderRequest.kt` contains a concrete field for resolved provider params
    - `ProviderCache.kt` exposes TTL-based read/write plus targeted invalidate behavior
    - `MenuService.kt` resolves `provider.params` through `PlaceholderPipeline` before provider invocation
    - `MenuService.kt` uses provider results and rerenders through `renderOpenMenuIfCurrent`
    - the automated verify command passes under the repo-local Java 21 toolchain
  </acceptance_criteria>
  <done>P5-01 and P5-05 are implemented through a registry/cache/runtime layer instead of ad hoc provider logic inside the parser or listeners.</done>
</task>

</tasks>

<verification>
- [ ] RED tests for provider cache and scheduler-safe rerender exist before implementation
- [ ] `mvn -q -Dtest=MenuDynamicProviderServiceTest,MenuServiceCompatibilityTest test` passes under Java 21
- [ ] provider cache keys are viewer/menu/surface/provider scoped
- [ ] targeted refresh invalidates only the intended provider-backed surface
- [ ] async provider completion returns through the platform-safe scheduler before rerender
</verification>

<success_criteria>
- Provider-backed page regions are resolved through a registry and cache facade rather than hardcoded `MenuService` branches.
- Viewer-scoped TTL caching and explicit invalidation are observable in regression tests.
- The runtime keeps Bukkit/Paper/Folia safety boundaries intact while adding async provider work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-page-provider-placeholderapi/05-02-SUMMARY.md`
</output>
