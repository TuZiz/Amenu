---
phase: 05-page-provider-placeholderapi
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt
  - src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt
  - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
  - src/main/kotlin/cc/keer/amenu/service/DynamicRefreshController.kt
  - src/main/resources/menus/admin.yml
  - src/main/resources/menus/history.yml
  - src/main/resources/menus/showcase.yml
  - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt
  - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt
  - src/test/kotlin/cc/keer/amenu/service/MenuDynamicRefreshTest.kt
autonomous: false
requirements: [P5-03, P5-06]
user_setup: []
must_haves:
  truths:
    - "Buttons can change icon/name/lore or visibility on a timer without reopening the menu."
    - "Dynamic refresh tasks stop when a menu closes, reloads, quits, or is replaced."
    - "Bundled menus visibly demonstrate safe PlaceholderAPI presence handling, explicit fallback states, and in-place timed updates."
  artifacts:
    - path: "src/main/kotlin/cc/keer/amenu/service/DynamicRefreshController.kt"
      provides: "Single owner for repeating refresh handles and lifecycle cleanup."
      contains: "cancel"
    - path: "src/test/kotlin/cc/keer/amenu/service/MenuDynamicRefreshTest.kt"
      provides: "Regression coverage for timed reevaluation and cleanup behavior."
      contains: "MenuDynamicRefreshTest"
    - path: "src/main/resources/menus/showcase.yml"
      provides: "Bundled Phase 5 dynamic showcase with PlaceholderAPI-safe and timed-state examples."
      contains: "%player_name%"
  key_links:
    - from: "src/main/kotlin/cc/keer/amenu/service/DynamicRefreshController.kt"
      to: "src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt"
      via: "controller owns repeating task handles for active menu views"
      pattern: "runRepeatingFor"
    - from: "src/main/kotlin/cc/keer/amenu/service/MenuService.kt"
      to: "src/main/kotlin/cc/keer/amenu/service/DynamicRefreshController.kt"
      via: "timed reevaluation invalidates dynamic state and rerenders in place"
      pattern: "renderOpenMenuIfCurrent|DynamicRefreshController"
---

<objective>
Finish the viewer-facing Phase 5 behavior: timed button reevaluation, lifecycle-safe refresh cleanup, bundled dynamic examples, and the PlaceholderAPI smoke gate.

Purpose: prove the new provider/pipeline work behaves like a stable menu engine feature rather than a parser-only capability.
Output: dynamic refresh controller, timed reevaluation regression coverage, bundled dynamic showcase updates, and a blocking manual PlaceholderAPI verification checkpoint.
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
@.planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
@.planning/phases/05-page-provider-placeholderapi/05-VALIDATION.md
@.planning/phases/05-page-provider-placeholderapi/05-01-PLAN.md
@.planning/phases/05-page-provider-placeholderapi/05-02-PLAN.md
@src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt
@src/main/kotlin/cc/keer/amenu/service/MenuService.kt
@src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt
@src/main/resources/menus/showcase.yml
@src/main/resources/menus/history.yml
@src/main/resources/menus/admin.yml

<interfaces>
From `src/main/kotlin/cc/keer/amenu/service/MenuService.kt`:
```kotlin
private fun renderOpenMenuIfCurrent(player: Player, menuId: String)
private fun clearHistory(player: Player)
```

From `src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt`:
```kotlin
class MenuListener(
    private val menuService: MenuService,
) : Listener
```

From `src/main/kotlin/cc/keer/amenu/platform/PlatformScheduler.kt`:
```kotlin
fun runRepeatingFor(player: Player, delayTicks: Long, periodTicks: Long, task: Runnable): TaskHandle
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for timed reevaluation, cleanup, and explicit fallback states</name>
  <files>src/test/kotlin/cc/keer/amenu/service/MenuDynamicRefreshTest.kt, src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt, src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt</files>
  <read_first>
    - .planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
    - .planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
    - .planning/phases/05-page-provider-placeholderapi/05-VALIDATION.md
    - .planning/phases/05-page-provider-placeholderapi/05-02-PLAN.md
    - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
    - src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt
    - src/main/resources/menus/showcase.yml
    - src/main/resources/menus/history.yml
    - src/main/resources/menus/admin.yml
  </read_first>
  <behavior>
    - Test `button_state_rechecks_without_menu_reopen`: a button with `update.interval: 20` changes state after the underlying permission or provider placeholder changes, and the player remains in the same open inventory.
    - Test `refresh_handles_are_cancelled_on_close_reload_quit_and_menu_replace`: repeating tasks stop when the inventory closes, the player quits, the plugin reloads, or another menu replaces the current one.
    - Test `provider_error_and_empty_states_render_explicit_fallback_items`: provider-backed surfaces render `error`/`empty` states instead of silently disappearing or crashing.
  </behavior>
  <action>Create `MenuDynamicRefreshTest.kt` plus the minimum RED assertions in `MenuRuntimeActionTest.kt` and `BundledMenuCompatibilityTest.kt` to cover timed reevaluation, lifecycle cleanup, and explicit `error`/`empty` rendering. Use the exact config key `update.interval: 20` in at least one test fixture. Keep the tests RED until the refresh controller and bundled showcase changes exist.</action>
  <verify>
    <automated>$env:JAVA_HOME='D:\codex\Amenu\.tools\jdk21'; $env:PATH=&quot;$env:JAVA_HOME\bin;$env:PATH&quot;; mvn -q -Dtest=MenuDynamicRefreshTest,MenuRuntimeActionTest,BundledMenuCompatibilityTest test; if ($LASTEXITCODE -eq 0) { throw 'Expected RED tests for Phase 5 Task 1 to fail before timed refresh implementation.' }</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/kotlin/cc/keer/amenu/service/MenuDynamicRefreshTest.kt` exists
    - the RED tests assert `update.interval: 20`, in-place state changes, and cleanup on close/reload/quit/menu switch
    - bundled-menu tests mention `%player_name%` or other PlaceholderAPI-safe demo strings
    - the RED verify command fails because refresh lifecycle support is not implemented yet
  </acceptance_criteria>
  <done>Wave 0 test scaffolds for P5-03 and the automated side of P5-06 exist and fail for the expected missing implementation reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the refresh controller, lifecycle cleanup, and bundled dynamic showcase</name>
  <files>src/main/kotlin/cc/keer/amenu/AMenuPlugin.kt, src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt, src/main/kotlin/cc/keer/amenu/service/MenuService.kt, src/main/kotlin/cc/keer/amenu/service/DynamicRefreshController.kt, src/main/resources/menus/admin.yml, src/main/resources/menus/history.yml, src/main/resources/menus/showcase.yml, src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt, src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt, src/test/kotlin/cc/keer/amenu/service/MenuDynamicRefreshTest.kt</files>
  <read_first>
    - .planning/phases/05-page-provider-placeholderapi/05-CONTEXT.md
    - .planning/phases/05-page-provider-placeholderapi/05-RESEARCH.md
    - src/test/kotlin/cc/keer/amenu/service/MenuDynamicRefreshTest.kt
    - src/test/kotlin/cc/keer/amenu/runtime/MenuRuntimeActionTest.kt
    - src/test/kotlin/cc/keer/amenu/runtime/BundledMenuCompatibilityTest.kt
    - src/main/kotlin/cc/keer/amenu/service/MenuService.kt
    - src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt
    - src/main/resources/menus/showcase.yml
    - src/main/resources/menus/history.yml
    - src/main/resources/menus/admin.yml
  </read_first>
  <action>Create `DynamicRefreshController.kt` as the single owner of repeating task handles per active viewer/menu/surface, and wire it into `MenuService.kt` so timed reevaluation invalidates the targeted provider/button state and then calls `renderOpenMenuIfCurrent(...)` instead of `openMenu(...)` per D-10 and the Phase 5 research pitfall. Add cleanup hooks in `MenuListener.kt` for inventory close and player quit, and cancel everything on reload/disable through `MenuService`/`AMenuPlugin.kt`. Keep this task runtime-focused: only touch the three bundled menus that prove the behavior. `showcase.yml` must include a PlaceholderAPI-safe `%player_name%` line, a stateful button that reevaluates on `update.interval: 20`, and a refresh/invalidation entrypoint; `admin.yml` should demonstrate permission-driven timed state switching that can be toggled by `op`/`deop`; `history.yml` should demonstrate explicit `loading`, `empty`, and `error` presentation for provider-backed regions. Do not expand this task into README or extra documentation churn.</action>
  <verify>
    <automated>$env:JAVA_HOME='D:\codex\Amenu\.tools\jdk21'; $env:PATH=&quot;$env:JAVA_HOME\bin;$env:PATH&quot;; mvn -q -Dtest=MenuDynamicRefreshTest,MenuRuntimeActionTest,BundledMenuCompatibilityTest test</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/kotlin/cc/keer/amenu/service/DynamicRefreshController.kt` exists and owns register/cancel cleanup behavior
    - `src/main/kotlin/cc/keer/amenu/gui/MenuListener.kt` contains close/quit cleanup hooks
    - `src/main/resources/menus/showcase.yml` contains `%player_name%` and `update.interval`
    - timed refresh rerenders in place instead of reopening the menu
    - the automated verify command passes under the repo-local Java 21 toolchain
  </acceptance_criteria>
  <done>P5-03 is implemented and the automated stability side of P5-06 is covered by tests and bundled examples.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the PlaceholderAPI-enabled Paper/Folia smoke gate</name>
  <files>src/main/resources/plugin.yml, src/main/resources/menus/showcase.yml, src/main/resources/menus/admin.yml, src/main/resources/menus/history.yml</files>
  <read_first>
    - .planning/phases/05-page-provider-placeholderapi/05-VALIDATION.md
    - src/main/resources/plugin.yml
    - src/main/resources/menus/showcase.yml
    - src/main/resources/menus/admin.yml
    - src/main/resources/menus/history.yml
  </read_first>
  <action>Do not change code in this task. First confirm Task 2 artifacts are present and the automated dynamic-refresh suite is still green. Then run the live PlaceholderAPI smoke exactly as written below on Paper or Folia: verify `%player_name%` resolution, verify timed button reevaluation after `op <playerName>`, and verify targeted refresh/invalidation without a full menu reopen. Capture failing step numbers and the observed runtime symptom if any check fails.</action>
  <what-built>PlaceholderAPI soft-bridge presence path, cache invalidation entrypoint, and timed button reevaluation without menu reopen.</what-built>
  <how-to-verify>
    1. Install PlaceholderAPI into a Paper or Folia test server that already has the freshly-built AMenu jar.
    2. Join the server as a normal player and run `/amenu open showcase`.
    3. Confirm the `%player_name%`-backed line in `showcase.yml` renders your real name instead of the raw token.
    4. Keep the menu open and, from console, run `op &lt;playerName&gt;`; wait one update interval (20 ticks / about 1 second); confirm the timed stateful button in `showcase` or `admin` changes icon/name/lore without closing or reopening the inventory.
    5. Trigger the bundled refresh/invalidation action from the dynamic showcase and confirm only the targeted provider-backed surface rerenders while the menu stays open.
    6. If testing on Folia, confirm no scheduler/thread violation appears in the server log during the timed update or refresh path.
  </how-to-verify>
  <resume-signal>Type `approved` if all six checks pass, or reply with the failing step numbers plus the observed behavior.</resume-signal>
  <verify>
    <automated>$env:JAVA_HOME='D:\codex\Amenu\.tools\jdk21'; $env:PATH=&quot;$env:JAVA_HOME\bin;$env:PATH&quot;; mvn -q -Dtest=MenuDynamicRefreshTest,MenuRuntimeActionTest,BundledMenuCompatibilityTest test</automated>
  </verify>
  <acceptance_criteria>
    - PlaceholderAPI presence path resolves `%player_name%` on a live Paper or Folia server
    - the timed button changes state after `op &lt;playerName&gt;` without reopening the inventory
    - the refresh/invalidation action rerenders only the target surface and keeps the menu stable
    - no scheduler/thread violation appears during the live smoke
  </acceptance_criteria>
  <done>P5-06 live smoke is explicitly approved or returned with concrete failing-step feedback.</done>
</task>

</tasks>

<verification>
- [ ] RED tests for timed reevaluation and cleanup exist before implementation
- [ ] `mvn -q -Dtest=MenuDynamicRefreshTest,MenuRuntimeActionTest,BundledMenuCompatibilityTest test` passes under Java 21
- [ ] timed button reevaluation uses in-place rerender instead of full menu reopen
- [ ] refresh handles clean up on close, reload, quit, and menu replacement
- [ ] PlaceholderAPI live smoke passes on Paper or Folia
</verification>

<success_criteria>
- Players can keep a menu open and watch button state change on schedule without losing page/history context.
- Dynamic refresh tasks never outlive the open menu lifecycle.
- The project has one concrete Paper/Folia smoke gate for the PlaceholderAPI presence path instead of an implied compatibility claim.
</success_criteria>

<output>
After completion, create `.planning/phases/05-page-provider-placeholderapi/05-03-SUMMARY.md`
</output>
